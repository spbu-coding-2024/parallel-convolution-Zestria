# Image Convolution

The `main` branch is intentionally kept minimal.  
Individual implementations live in feature branches:

| Branch | Description |
|--------|-------------|
| `task1` | Sequential image convolution |
| `task2` | Parallel convolution (extends sequential) |